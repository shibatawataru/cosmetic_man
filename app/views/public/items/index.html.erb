<h1>商品一覧（全 <%= @quantity %> 件）</h1>
 <% @items.each do |item| %>
  <%= link_to public_item_path(item.id) do %>
    <%= image_tag item.get_product_image(200,200)%>
  <% end %><br>
    <%= item.itemname %><br>
    <%= item.price %>円
  
  <% end %>