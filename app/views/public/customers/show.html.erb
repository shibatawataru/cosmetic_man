 <div class='container'>
   <div class="row">
     <div class="col-3">
      <h2>マイページ</h2>
      <%= render 'public/customers/customerinfo', customer: @customer %>
     </div>
          <% if @customer %>
          <%= link_to "+", new_public_item_path, class: "btn btn-default bg-primary text-white", style: "border-radius: 100%" %>
        <% end %>
      <div class="mx-auto col-9" style="display: flex;">
   

      <% @item.each do |item| %>
         <div class="col-md-4">
          <div class="card mb-3">
           <%= image_tag item.product_image, class: 'bd-placeholder-img card-img-top product-card-image' %>
            <div class="card-body">
             <h5 class="card-title"><%= link_to item.itemname, public_item_path(item.id) %></h5>
              <div class="card-text">
               <%= simple_format(item.body) %>
              </div>
              <div class="card-integer">
               <%= item.price %>円
              </div>
              <div class="item-evaluation-<%= item.id %>" data-score="<%= item.evaluation %>">
               <script>
                $('.item-evaluation-<%= item.id %>').empty();
                $('.item-evaluation-<%= item.id %>').raty({
                  readOnly: true,
                  score: function() {
                    return $(this).attr('data-score');
                  },
                  starOff: '<%= asset_path('star-off.png') %>',
                  starOn: '<%= asset_path('star-on.png') %>',
                  starHalf: '<%= asset_path('star-half.png') %>',
                });
               </script>
              </div>
             </div>
            </div>
          </div>
        <% end %>
       </div>
     </div>
    </div>